# Resend Email API (REQUIRED for sending configuration emails)
# Sign up at https://resend.com
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=onboarding@kairen.xyz

# Public URL (for email links)
NEXT_PUBLIC_BASE_URL=https://kairen.xyz

# Note: Rate limiting uses in-memory storage by default (works for single server)
# If you need distributed rate limiting across multiple servers, you can optionally add Upstash Redis
